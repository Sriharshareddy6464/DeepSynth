Archived Flask-rendered UI assets live here during Phase 1 frontend unification.

- `legacy/templates` contains the old Jinja templates.
- `legacy/static` contains the old static assets that were tied to the Flask UI.
- React (`frontend/`) is now the active frontend.
- Flask (`server.py`) is now API-only.
